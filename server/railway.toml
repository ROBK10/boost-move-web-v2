[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build && npx prisma generate --schema src/prisma/schema.prisma"

[deploy]
startCommand = "npx prisma migrate deploy --schema src/prisma/schema.prisma && npm start"
restartPolicyType = "on_failure"
